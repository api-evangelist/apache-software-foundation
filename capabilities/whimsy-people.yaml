naftiko: 1.0.0-alpha2
info:
  label: Apache Software Foundation Whimsy Public Data API — People
  description: 'Apache Software Foundation Whimsy Public Data API — People. 1 operations. Lead operation: Apache Software
    Foundation Get LDAP People Data. Self-contained Naftiko capability covering one Apache Software Foundation business surface.'
  tags:
  - Apache Software Foundation
  - People
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    APACHE_SOFTWARE_FOUNDATION_API_KEY: APACHE_SOFTWARE_FOUNDATION_API_KEY
capability:
  consumes:
  - type: http
    namespace: whimsy-people
    baseUri: https://whimsy.apache.org/public
    description: Apache Software Foundation Whimsy Public Data API — People business capability. Self-contained, no shared
      references.
    resources:
    - name: public_ldap_people.json
      path: /public_ldap_people.json
      operations:
      - name: getpublicldappeople
        method: GET
        description: Apache Software Foundation Get LDAP People Data
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: whimsy-people-rest
    port: 8080
    description: REST adapter for Apache Software Foundation Whimsy Public Data API — People. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/public-ldap-people-json
      name: public-ldap-people-json
      description: REST surface for public_ldap_people.json.
      operations:
      - method: GET
        name: getpublicldappeople
        description: Apache Software Foundation Get LDAP People Data
        call: whimsy-people.getpublicldappeople
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: whimsy-people-mcp
    port: 9090
    transport: http
    description: MCP adapter for Apache Software Foundation Whimsy Public Data API — People. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: apache-software-foundation-get-ldap
      description: Apache Software Foundation Get LDAP People Data
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: whimsy-people.getpublicldappeople
      outputParameters:
      - type: object
        mapping: $.
